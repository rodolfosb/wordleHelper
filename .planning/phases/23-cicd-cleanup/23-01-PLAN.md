---
phase: 23-cicd-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, README.md]
autonomous: false
---

<objective>
Remove gh-pages dependency and update project documentation for Vercel deployment.

Purpose: Clean up legacy GitHub Pages deployment artifacts now that the app is deployed on Vercel at wordle.brdk.cc.
Output: Cleaner package.json without unused dependencies, comprehensive README.md documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-pwa-updates/22-01-SUMMARY.md
@package.json
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove gh-pages dependency and deploy script</name>
  <files>package.json</files>
  <action>
    1. Remove `gh-pages` from devDependencies
    2. Remove the `deploy` script that uses gh-pages (`npm run build && gh-pages -d dist`)
    3. Optionally add a comment or note that deployment is handled by Vercel
  </action>
  <verify>npm install succeeds, package.json no longer contains gh-pages</verify>
  <done>gh-pages removed from dependencies, deploy script removed</done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive README.md</name>
  <files>README.md</files>
  <action>
    Create README.md with:
    1. Project title and description (from PROJECT.md core value)
    2. Live demo link: https://wordle.brdk.cc
    3. Features list (all validated requirements from PROJECT.md)
    4. Tech stack (Vite, TypeScript, vanilla DOM, vite-plugin-pwa)
    5. Local development instructions:
       - `npm install` to install dependencies
       - `npm run dev` to start development server
       - `npm run build` to create production build
    6. Deployment note: Auto-deploys to Vercel on push to main
    7. PWA installation instructions

    Keep it concise and useful. No emojis unless project already uses them.
  </action>
  <verify>README.md exists, cat README.md shows all sections</verify>
  <done>README.md created with all required sections</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cleaned up package.json and created README.md documentation</what-built>
  <how-to-verify>
    1. Review README.md content in the repo
    2. Push changes to main branch
    3. Visit Vercel dashboard or wait 1-2 minutes
    4. Confirm deployment triggered automatically
    5. Visit https://wordle.brdk.cc and verify site works
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install` succeeds without gh-pages
- [ ] `npm run build` still works
- [ ] README.md exists with all sections
- [ ] Vercel auto-deployment verified
</verification>

<success_criteria>

- gh-pages dependency removed
- Deploy script removed from package.json
- README.md created with comprehensive documentation
- Vercel auto-deployment confirmed working
- No breaking changes introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/23-cicd-cleanup/23-01-SUMMARY.md`
</output>

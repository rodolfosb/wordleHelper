---
phase: 04-user-interface
plan: 01
type: execute
depends_on: []
files_modified: [src/style.css, src/main.ts, src/ui/GuessGrid.ts]
---

<objective>
Build the color-coded guess input interface that lets users enter letters and mark feedback.

Purpose: Enable users to input their Wordle guesses and mark each letter as green/yellow/gray, matching the mental model of Wordle feedback.
Output: A 6-row guess grid with clickable color cycling and keyboard input for letters.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/02-core-logic/02-01-SUMMARY.md
@src/types/index.ts
@src/main.ts
@src/style.css

**Tech stack:** Vanilla TypeScript (no React/Vue), Vite build
**Established patterns:** Types in src/types/, logic in src/logic/, data in src/data/
**Key types:** LetterStatus ('green' | 'yellow' | 'gray'), LetterFeedback, GuessFeedback
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guess grid HTML structure and base styles</name>
  <files>src/main.ts, src/style.css, index.html</files>
  <action>
    Replace the placeholder HTML in main.ts with a proper guess grid:
    - App container with header, guess grid area, and suggestions area (placeholder for Plan 02)
    - Guess grid: 6 rows (max guesses), 5 cells per row (5 letters)
    - Each cell: 50x50px, border, centered letter display, data attributes for row/position
    - Initial state: all cells empty and gray background
    - CSS: Use CSS Grid for layout. Colors: gray (#787c7e), yellow (#c9b458), green (#6aaa64), white text on colored cells
    - Add Wordle-inspired styling (clean, minimal, readable)
  </action>
  <verify>npm run dev shows 6x5 grid of cells with gray borders, no console errors</verify>
  <done>Grid displays correctly with 30 cells arranged 6 rows × 5 columns</done>
</task>

<task type="auto">
  <name>Task 2: Implement letter input with keyboard navigation</name>
  <files>src/ui/GuessGrid.ts, src/main.ts</files>
  <action>
    Create GuessGrid class to manage input state and DOM updates:
    - Track current row (0-5) and current position (0-4)
    - On keypress A-Z: fill current cell, advance position (wrap to next row at position 5)
    - On Backspace: clear current cell, move back (stop at position 0)
    - On Enter: "submit" current row (only if row is full) - emit custom event or callback
    - Focus management: auto-focus grid on page load, use tabindex
    - Export GuessGrid class from src/ui/GuessGrid.ts
    - Wire up in main.ts: create instance, attach to DOM
    - DO NOT advance to next row on Enter yet - that will be wired in Plan 02 with constraint logic
  </action>
  <verify>Type letters, see them appear in cells. Backspace removes. Cannot type more than 5 letters per row.</verify>
  <done>Keyboard input works: A-Z fills cells, Backspace removes, Enter does nothing visible yet (logs or emits event)</done>
</task>

<task type="auto">
  <name>Task 3: Implement color cycling on cell click</name>
  <files>src/ui/GuessGrid.ts, src/style.css</files>
  <action>
    Add click handler for cells in current/completed rows:
    - Click cycles: gray → yellow → green → gray
    - Store color state per cell (use LetterStatus type)
    - Update cell background color and add/remove CSS classes
    - Only allow color change on cells that have letters
    - Cells in future rows (not yet typed) are not clickable
    - Add method to get current row as GuessFeedback array (for constraint building)
    - CSS classes: .cell-gray, .cell-yellow, .cell-green with appropriate background colors
  </action>
  <verify>Click a filled cell, background cycles through colors. Click empty cell, nothing happens.</verify>
  <done>Color cycling works on filled cells, getGuessFeedback() returns correct GuessFeedback array</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] 6x5 grid displays on page load
- [ ] Typing letters fills cells left-to-right
- [ ] Backspace removes letters right-to-left
- [ ] Clicking filled cells cycles colors (gray → yellow → green → gray)
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- GuessGrid class properly typed and exported
- Ready for Plan 02 to wire up suggestions display
</success_criteria>

<output>
After completion, create `.planning/phases/04-user-interface/04-01-SUMMARY.md`
</output>
